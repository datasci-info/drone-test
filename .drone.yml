kind: pipeline
name: first

steps:
- name: yee
  image: alpine
  commands:
  - echo "run one"
  - env
  - echo "$${PLUGIN_TEST}"
  settings:
    test: "123123"
  when:
    event:
      include:
      - push
